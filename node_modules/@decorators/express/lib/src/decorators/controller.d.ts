import { RouterOptions } from 'express';
import { Middleware } from '../middleware';
/**
 * Registers controller for base url
 */
export declare function Controller(url: string, middleware?: Middleware[]): ClassDecorator;
export declare function Controller(url: string, routerOptions?: RouterOptions, middleware?: Middleware[]): ClassDecorator;
